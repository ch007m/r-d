---
registry:
  server: "registry.pivotal.io"
  username: "<VMWARE_USERNAME>"
  password: "<VMWARE_PASSWORD>"

provider: local
pdb:
  enable: "true"

ingress:
  reuse_crds:
  external:
    namespace:
  internal:
    namespace:

local_dns:
  enable: "false"