#@ load("@ytt:overlay", "overlay")

#@overlay/match by=overlay.subset({"kind":"Deployment", "metadata":{"name":"kpack-controller"}}),expects=1
---
spec:
  template:
    spec:
      #@overlay/match missing_ok=True
      volumes:
        - name: custom-certs
          secret:
            secretName: cert-key